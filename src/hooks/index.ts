/**
 * Custom React hooks for data fetching and state management.
 * All Supabase queries go through hooks in this directory.
 */

// This file serves as an index. Individual hooks will be created per domain (DSA, Applications, etc.)
// Example hooks to come in Phase 5+:
// - useDSAProblems() — fetch all DSA problems for the user
// - useApplications() — fetch all applications
// - useWeeklyProgress() — calculate weekly stats
// - usePhaseInfo() — get current phase and related data
// - useUserSettings() — get/update user preferences
// - useGeminiCoach() — manage AI coach state and caching
